name: build

defaults:
  run:
    working-directory: rust-lib

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    tags: [ v* ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: read
  id-token: write

jobs:

  rust-and-wasm:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: echo github ref
      run: echo "${{ github.ref }}"
    
    - name: Install
      run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
    
    - name: Build Rust
      run: cargo build --verbose --release
    
    - name: Run tests
      run: cargo test --verbose
    
    - name: Build docs
      run: cargo doc --no-deps
    
    - name: test WASM
      run: wasm-pack test --headless --chrome # --firefox # Disabling tests in FF temporarily (refer: https://github.com/rustwasm/wasm-bindgen/issues/2261)
    
    - name: build WASM bundler
      run: wasm-pack build --out-dir ../npm-packages/wasm/dist/bundler --release

    - name: build WASM cjs
      run: wasm-pack build --target nodejs --out-dir ../npm-packages/wasm/dist/cjs --release

    - name: cleanup dist
      run: rm ../npm-packages/wasm/dist/bundler/.gitignore ../npm-packages/wasm/dist/bundler/email_address_parser.d.ts ../npm-packages/wasm/dist/bundler/LICENSE ../npm-packages/wasm/dist/bundler/package.json  ../npm-packages/wasm/dist/bundler/README.md ../npm-packages/wasm/dist/cjs/.gitignore ../npm-packages/wasm/dist/cjs/email_address_parser.d.ts ../npm-packages/wasm/dist/cjs/LICENSE ../npm-packages/wasm/dist/cjs/package.json  ../npm-packages/wasm/dist/cjs/README.md

    - uses: actions/setup-node@v1
      with:
        node-version: '24.x'
        registry-url: https://registry.npmjs.org/
        scope: sparser

    - name: npm ci (workspace)
      run: npm ci
      working-directory: ./npm-packages

    - name: test wasm npm package
      run: npm test --workspace wasm
      working-directory: ./npm-packages

    # publish to crates.io
    - uses: rust-lang/crates-io-auth-action@v1
      id: auth
    - name: crates.io publish
      run: cargo publish
      env:
        CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
      if: startsWith(github.ref, 'refs/tags/v')

    # publish to npm
    - name: publish to npm
      run: npm publish --access public --@sparser:registry=https://registry.npmjs.org/ --tag latest
      working-directory: ./npm-packages/wasm
      if: startsWith(github.ref, 'refs/tags/v')

  regex-package:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: npm-packages

    steps:
    - uses: actions/checkout@v2

    - name: echo github ref
      run: echo "${{ github.ref }}"

    - uses: actions/setup-node@v1
      with:
        node-version: '24.x'
        registry-url: https://registry.npmjs.org/
        scope: sparser

    - name: npm ci (workspace)
      run: npm ci

    - name: build regex package
      run: npm run build --workspace regex

    - name: test regex package
      run: npm test --workspace regex

    - name: publish regex package to npm
      run: npm publish --access public --@sparser:registry=https://registry.npmjs.org/ --tag latest
      working-directory: ./npm-packages/regex
      if: startsWith(github.ref, 'refs/tags/v')
